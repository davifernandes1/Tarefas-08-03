import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: 24,
    height: 24,    
    alignItems: 'center',
    justifyContent: 'center',    
    borderRadius: 12,
    borderWidth: 2,
    marginRight: 8,
  },

  icon: {
    width: 34,
    height: 34,
    alignItems: 'center',
    justifyContent: 'center',
  }
})